apiVersion: v1
kind: Service
metadata:
  name: minipay-db
  namespace: minipay
spec:
  selector:
    app: minipay-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless: stable DNS for the single DB pod
